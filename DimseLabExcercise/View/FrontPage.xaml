<Page
    x:Class="DimseLabExcercise.View.FrontPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DimseLabExcercise.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Pivot x:Name="rootPivot" Title="Category Title">
            <PivotItem Header="Project">
              <ListView ItemsSource="{Binding ProjectCollection}">
                
              </ListView>
            </PivotItem>
            <PivotItem Header="Participants">
                <ListView ItemsSource="{Binding ParticipantCollection}"></ListView>
            </PivotItem>
            <PivotItem Header="Thing">
                <ListView ItemsSource="{Binding ThingCollection}"></ListView>
            </PivotItem>
        </Pivot>

    </Grid>
</Page>
